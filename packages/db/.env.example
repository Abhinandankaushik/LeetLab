# Example environment file for the @repo/db package (Prisma).
# Copy to .env when running prisma CLI commands (generate/migrate/studio)
# directly from this package. Apps provide DATABASE_URL via their own env.

# PostgreSQL connection string
DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_NAME>"
